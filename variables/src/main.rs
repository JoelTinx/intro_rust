fn main() {
	const PI: f64 = 3.1415;
	let mut first_number: f64 = 10.0;
	let second_number: f64 = 20.0;
	let result = PI + first_number + second_number;
	println!("The result of the adition {} + {} + {} = {}", PI, first_number, second_number, result);
}
